const INITIAL_STATE = {
    info: 'sample about'
};

const aboutReducer = (state = INITIAL_STATE, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default aboutReducer;